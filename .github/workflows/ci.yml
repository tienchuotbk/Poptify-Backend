name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      mysql:
        image: mysql:8
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: poptify
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -uroot -proot"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=10

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci
      - run: npm run lint
      - run: npm run build
      - run: npm test
      - run: npm run test:e2e

      # Migration chạy trên MySQL service container (task 4.5).
      # Baseline migration thêm ở task 6.1 (gộp 1.5): tạo 3 bảng bootstrap.
      # Verify cả up + down: run → revert (down sạch) → run lại (up idempotent).
      - name: Migrations up/down/up (MySQL)
        run: |
          npm run migration:run
          npm run migration:revert
          npm run migration:run
        env:
          NODE_ENV: production
          DB_HOST: 127.0.0.1
          DB_PORT: 3306
          DB_USERNAME: root
          DB_PASSWORD: root
          DB_NAME: poptify

      # TODO(task 4.5): bật blocking sau khi giải quyết hết high/critical vuln.
      - name: Security audit (non-blocking tạm thời)
        run: npm audit --audit-level=high
        continue-on-error: true

  # ---- Build & push Docker image lên GitHub Container Registry (ghcr.io) ----
  docker:
    needs: build
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - name: Image name (lowercase, ghcr yêu cầu)
        id: meta
        run: echo "image=ghcr.io/${GITHUB_REPOSITORY,,}" >> "$GITHUB_OUTPUT"
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }} # tự động, không cần thêm
      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: |
            ${{ steps.meta.outputs.image }}:latest
            ${{ steps.meta.outputs.image }}:${{ github.sha }}

  # ---- Deploy: SSH lên VPS → cập nhật image cho Deployment k3s + chờ rollout ----
  # Yêu cầu trên VPS (1 lần): đã `kubectl apply -f poptify-k3s.yaml` + tạo Secret
  # `poptify-backend-env`, và VPS_USER có kubeconfig (vd: ~/.kube/config copy từ
  # /etc/rancher/k3s/k3s.yaml) để chạy `kubectl` không cần sudo.
  deploy:
    needs: docker
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to k3s over SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e
            IMAGE="ghcr.io/$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')"
            # Set image về tag SHA (immutable) → đảm bảo pod kéo đúng bản vừa build
            kubectl -n default set image deployment/poptify-backend backend="${IMAGE}:${{ github.sha }}"
            kubectl -n default rollout status deployment/poptify-backend --timeout=180s
