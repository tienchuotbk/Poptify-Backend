/** Enum dùng chung cho Announcement Bar (entity + DTO). */

export enum AnnouncementBarType {
  Simple = 'simple',
  Countdown = 'countdown',
  FreeShippingProgress = 'free_shipping_progress',
}

export enum AnnouncementBarPosition {
  Top = 'top',
  Bottom = 'bottom',
}
