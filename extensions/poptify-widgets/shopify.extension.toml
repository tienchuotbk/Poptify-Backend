api_version = "2026-04"

# Theme App Extension cho Poptify (storefront UI — buyer-facing).
# Deploy bằng Shopify CLI (`shopify app deploy`); cần shopify.app.toml (task 0.5/6.2).
[[extensions]]
type = "theme"
name = "poptify-widgets"
handle = "poptify-widgets"
